# 原型开发说明

请自行运行本地服务器，并在当前环境可用的浏览器中打开预览。能够代为运行时，不要只向用户提供启动命令。

进行较大的视觉调整前，如果视觉来源不明确或不再符合当前目标，请使用 Product Design 插件的 `get-context` 技能。当用户给出可长期沿用的原型设计反馈、偏好或决策时，将其记录到 `AGENTS.md`。

基于用户选中的生成图实现时，应把该图视为布局、组件结构、密度、间距、颜色、字体、可见内容和信息层级的视觉基准。

应用界面代码放在 `src/`。保持 `.openai/hosting.json`、`worker/index.js`、`scripts/prepare-sites-build.mjs` 和 `tests/sites-worker.test.mjs` 不变，以便将同一本地原型交接给 Sites。交接前运行 `npm run build` 和 `npm run test:sites`；构建结果必须包含 `dist/client/index.html`、`dist/server/index.js` 和 `dist/.openai/hosting.json`。

## 长期设计决策

- 已选定的主视觉是 Product Design 方案 3，保存在 `docs/selected-design.png`。
- OrbitBit 必须与 VPet 保持明显区别：不得复制 VPet 的代码、角色美术、动画帧、名称或标志。
- 陪伴角色采用米白色球形外壳、深色面罩、青色眼睛和橙色点缀；核心循环是完成微任务并点亮七星图。
- 角色美术、壁纸、内容包和状态逻辑应相互分离，使贡献者无需修改渲染外壳即可扩展应用。
- Windows 打包版必须是真正透明的悬浮宠物，不显示全窗口壁纸或不透明根背景；壁纸和桌面装饰只保留在浏览器展示预览中。
- 已选定的动效方向是 Product Design 方案 3，保存在 `docs/selected-motion-design.png`：默认采用安静陪伴模式，窗口基准为紧凑的 420×470；控件在靠近或聚焦时出现，透明像素应允许点击穿透到真实桌面。
- 通过轻微的光标注意、触碰回应和短促的点星完成仪式建立健康情感连接。不得使用断签损失、愧疚提示、虚假紧迫感、随机奖励、侵入式提醒或其他焦虑驱动的依赖模式。
- 用户可通过右下角的轻量把手自由缩放桌宠，宽度限制为保证可读性的 340–600 像素，并保持固定比例。应用需要记住上次大小，并在托盘菜单提供“小巧 / 标准 / 舒展”预设。
- 项目 Markdown 文档统一使用中文；标准许可证法律文本保持原文。
